// This route has been replaced by NextAuth at /api/auth/[...nextauth]/route.ts
// Credentials login is now handled via next-auth CredentialsProvider.
// Google SSO is handled via next-auth GoogleProvider.
// See: app/lib/auth.ts for the full configuration.
export {}
